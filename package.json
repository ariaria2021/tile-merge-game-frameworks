{
  "name": "tile-merge-game-frameworks",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev:react": "npm run dev -w @game-frameworks/react",
    "dev:vue3": "npm run dev -w @game-frameworks/vue3",
    "dev:svelte": "npm run dev -w @game-frameworks/svelte",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {}
}